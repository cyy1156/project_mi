# =============================================================================
# 模型登记表 — 操作员: model list / model start <顶层键名>
# =============================================================================

demo:
  说明: 内置演示，打印均值方差，无需权重文件
  窗口采样点数: 250
  步长采样点数: 125
  模块: models.demo_stats
  类名: DemoStatsModel

# 以下示例模型文件尚不存在，启动会失败；需要时取消注释并补全 models/*.py
# 睡眠检测:
#   说明: 示例 — 睡眠分期模型（需 models/sleep_model.py）
#   窗口采样点数: 250
#   步长采样点数: 125
#   模块: models.sleep_model
#   类名: SleepModel
#   权重文件: checkpoints/sleep.pt
#
# 运动想象:
#   说明: 示例 — 运动想象 2 秒窗口
#   窗口采样点数: 500
#   步长采样点数: 250
#   模块: models.mi_model
#   入口: predict
#   权重文件: checkpoints/mi.onnx
#   输入格式: channels_samples
