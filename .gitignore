# ================= 大文件 / 本地数据（不入库）=================
# 说明：experiment_game/data/（约 332MB）与训练输出/权重（约 490MB）
#      属本地专属数据与可复现的中间产物，一律不入库（本地保留）。
#      需要时见 README「数据与产物」一节。

# ---- 本地大数据集（公共数据集 / 本地二进制语料）----
# Anchor to repo-root only — on Windows, bare "DATA/" also matches **/data/
/DATA/
**/*.mat
**/*.gdf
**/*.bdf
**/*.edf

# ---- experiment_game/data：整目录不入库（原始采集数据 + 派生样本，约 332MB）----
# 本地专属数据，仓库不提交、不修改、不删除
experiment_game/data/
!experiment_game/data/.gitkeep

# ---- 训练输出 / 权重 / prob dump（约 490MB）----
code/train_lab/out/
code/adapt_engine/results/
**/out/
**/*.pt
**/*.pth
**/*.onnx
**/*.pkl
**/*.joblib
**/*.pid
**/System.Collections.Specialized.OrderedDictionary
**/*_X.npy
**/*_X_noz.npy
**/_cache_*.npy
*.npy

# ---- 日志与缓存 ----
**/*.log
**/__pycache__/
**/*.py[cod]
**/.venv/
**/venv/
*.egg-info/
.pytest_cache/

# ================= IDE / 编辑器 / OS =================
**/.idea/
**/.vscode/
*.swp
.trae/
.workbuddy/
experiment_game/.workbuddy/
.DS_Store
Thumbs.db

# Microsoft Office temporary lock files
~$*

# ================= 媒体 / 压缩包（GitHub 不宜托管）=================
**/*.mp4
**/*.MP4
**/*.mov
**/*.MOV
**/*.avi
**/*.mkv
**/*.zip
**/*.7z
**/*.rar
**/*.tar
**/*.tar.gz
**/*.tgz

# 图片默认忽略（避免仓库膨胀）；技术报告插图显式放行
*.png
!资料/初赛材料/01_技术报告/**/*.png
!资料/初赛材料/02_离线验证/截图/*.png
!资料/初赛材料/02_离线验证/交稿/**/*.png

# 初赛提交整包（含演示视频，与交稿目录重复）
资料/初赛材料/上海理工大学_面向少样本个性化的智能运动想象脑机交互系统/
资料/初赛材料/上海理工大学_面向少样本个性化的智能运动想象脑机交互系统.zip
# 演示视频目录仅本地保留成品
资料/初赛材料/03_演示视频/**

# ================= 其他 =================
# Local UI memory (COM / absolute paths); commit example only
experiment_game/config/operator_defaults.json

# Nested-repo backup (LSL_connect_model had its own .git)
_git_backup_LSL_connect_model/
project_mi/
