# 重构时机决策报告（2026-08-29）

> **阅读说明（2026-08-29 晚间同步）**  
> 本文保留「现在分轨重构」的决策论据；下列表述**已过期**，勿再当作现状：  
> 「F5 待定 / 双轨等数据」「通道序待定夺」「无基线提交 / 108 处未提交」。  
> **现行进度与执行范围以** [`重构实施方案_20260829.md`](重构实施方案_20260829.md) **v1.1 §0 为准**；规则以 [`框架冻结确认_20260829.md`](框架冻结确认_20260829.md) 为准。

---

## 0. 现状快照（与实施方案对齐）

| 项 | 状态（同步后） |
|----|----------------|
| F1–F24（含 **F5**） | ✅ 全部冻结；正式读出 = 因果平滑 + 多数票；配置默认 `primary_judge_mode=majority` |
| 通道序 | ✅ `FC3, C3, CP3, CZ, CPZ, FC4, C4, CP4`（设备=模型恒等）；数据已迁 |
| git 基线 | ✅ **`db08ed2`** 已 commit 并 push `origin/main`（阶段 0.1 完成） |
| 被试 FT | ✅ 实验 31 Leave-Next+F5 已 promote；旧 `current` 归档保留 |
| 结构债 | ❌ 仍在：反向依赖、`experiment_game/core/` 未建、循环导入、收尾兜底未系统化 |
| **下一步** | 按实施方案执行阶段 0.2–0.4 → 1 → 2 → 3（见该文 §2–§5） |

**结论（不变）**：规则前提已满足 → **现在做与规则无关的 A/B/C 结构重构**；总册中的 EEGBus / 机位配置 / SessionRunner 大拆等**不进本期**（实施方案 §0.1）。

---

## 1. 前提校正：规则并没有「未确认」

`docs/框架冻结确认_20260829.md` 已确认 **F1–F24 全部规则**（含 F5）：

| 项 | 状态 | 内容 |
|----|------|------|
| F1–F4 | ✅ 已冻结 | OpenBMI-Align 时序；Rest=专用 4s；零样本 = E1f 四成员 |
| **F5** | ✅ 已冻结 | **因果平滑 + 多数票单轨**；τ / `e1f_conf_stop` 退出正式 SOP |
| F6–F14 等 | ✅ 已冻结 | 计分；采后 Leave-Next；门控 FAIL 可强制晋升；replay 0.10 等 |

权威优先级：**配置 + 代码 > 冻结表 > 其它文档**；升级纪律：先改配置与测试，再改文档。  
只要不改实验语义，移动代码结构合规。

资料侧「20 被试流程 / 15+5 任务书 / 游戏 v2 计划」等页眉已标历史——对比规则请用冻结表，不要用那三份。

---

## 2. 代码现状（决策当日扫描 → 晚间订正）

### 2.1 已落地（可复用）

| 项 | 状态 |
|----|------|
| 原子写 `experiment/atomic_io.py` | ✅（待下沉 `experiment_game/core/`） |
| EEG 断流看门狗 / stale | ✅ `inference_v2` |
| FT 安全落盘 + 门控 | ✅ |
| E1f + F5 配置 | ✅ `readout_mode=e1f`，`primary_judge_mode=majority`（**不是**旧文所说的 conf_stop 默认） |

### 2.2 仍为工程风险（重构对象）

- 反向依赖：offline/acquisition → `experiment.channel_layout`；orchestrator → tools/offline  
- **`experiment_game/core/` 尚不存在**  
- 循环导入：`experiment/__init__.py` eager + `session` ← `acquisition`（`test_acq_health` 收集失败）  
- ~~无基线提交~~ → **已消除**（`db08ed2`）

---

## 3. 分轨决策（规则依赖度）— 同步后

| 轨道 | 内容 | 是否被规则卡住 | 决策（现行） |
|------|------|----------------|--------------|
| A. 基础设施 | `core/` 下沉、解反向依赖；orchestrator **本期不拆**主体（注入解环即可） | ❌ | **现在做**（实施方案阶段 1） |
| B. 数据安全 | 收尾兜底、events 容错、剩余原子写 | ❌ | **现在做**（阶段 2） |
| C. 切窗归一 | 多份实现 → `core/windowing` | ❌ 通道序**已定**；剩归一与回归锚点 | **现在做**（阶段 3） |
| D. F5 | 正式单轨已冻结 | — | **降级轻量收尾**（可选旁路标记）；**不再**「等战役再定双轨」 |
| 总册其余 | EEGBus / 机位 paths / SessionRunner… | — | **本期不做**（见实施方案 §0.1） |

---

## 4. 两方案利弊（历史论据，结论仍成立）

### 方案甲：现在重构（已采纳）

**利**：修订窗口内清结构债；A/B/C 与 F5 语义无关；战役前补收尾可降废会话。  
**弊**：仍需约数日工程 + 合成板冒烟。（~~无基线时动手风险~~ → 基线已有。）

### 方案乙：等规则再改（已否决）

原「等 F5」会造成与战役的循环等待；且 F5 **现已冻结**，方案乙失去最后一块挡板。

---

## 5. 影响评估（订正）

| 维度 | 现在分轨重构 |
|------|----------------|
| 规则风险 | 低（不动时序/计分语义） |
| 回归风险 | 有基线 `db08ed2` + 每阶段可 revert；须测绿 + 冒烟 |
| F5 | 已冻结；战役数据用于监测，**不再阻塞**结构重构 |
| 通道序 | 已定；C 轨只做切窗代码归一 |

---

## 6. 行动清单（与实施方案对齐；取代旧「0–5」过期条）

| # | 动作 | 状态 |
|---|------|------|
| 0.1 | git 基线提交并推送 | ✅ `db08ed2` |
| 0.2–0.4 | 修 F5 过期断言、断循环导入、测绿 → `phase0` | 待做 |
| 1 | A 轨：`experiment_game/core/` + 依赖方向测试 | 待做 |
| 2 | B 轨：会话收尾 + events 容错 + 原子写 | 待做 |
| 3 | C 轨：切窗归一 + 明确回归锚点 | 待做 |
| — | orchestrator 大拆 / 换机配置总册项 | **P2 / 战役后**（非本期） |

细则、验收门、战役检查单 → **[`重构实施方案_20260829.md`](重构实施方案_20260829.md) v1.1**。

---

## 7. 修订记录

| 日期 | 变更 |
|------|------|
| 2026-08-29 初版 | 论证「现在分轨重构」；当时 F5/通道/基线表述与午后局势混写 |
| 2026-08-29 晚间 | **同步**：F5 已冻、通道已定、基线已 push；过期句作废；行动清单对齐实施方案 v1.1 §0 |
