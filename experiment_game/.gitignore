# Temp / browser QA artifacts only.
# Session & epoch keep/exclude policy lives in the repo-root .gitignore
# (whitelists sub01/sub02/sub03 real collections).
data/_*.txt
data/_edge*
__pycache__/
*.pyc
.pytest_cache/

# 本机操作台记忆（含 COM / 绝对路径）；仓库只提交 example
config/operator_defaults.json
