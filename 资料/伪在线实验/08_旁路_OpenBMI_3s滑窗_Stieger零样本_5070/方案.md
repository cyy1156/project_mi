# 方案：shallow · 3s/hop100 · **Stieger 复现伪在线 01–06**（旁路）

> 状态：**5070 机位孪生 · 协议同 07** · 2026-08-21  
> **v1.1（2026-08-22）：新增 §3.5 · S08-01b / S08-02b = Tw=2s 同库对照臂**（窗长跨域迁移判定，必跑线 E）  
> 机位：**RTX 5070 Laptop** · 对照臂：[07（5060）](../07_旁路_OpenBMI_3s滑窗_Stieger零样本/)  
> 离线训练包：`5070_baselines_openbmi_3s_hop100_accpaper`  
> 臂名：`08_旁路_OpenBMI_3s滑窗_Stieger零样本_5070`  
> 模型归类：[按模型/shallow](../按模型/shallow/)  
> 离线母本：[模型训练/21_5070_旁路_shallow_3s…](../../模型训练/21_5070_旁路_shallow_3s滑窗100ms_openbmi_accpaper/)  
> **禁止**：覆盖 **07 / 01–06** 的 `results/`；写入 `out/5060_*`；改 Acc_paper 定义；用 2s 权重配 3s 窗；与 04/07 混比夺冠。

旧名「游戏零样本」仅跳转。**本臂伪在线数据一律 = Stieger**，不用游戏 / 不用 BCI2a 评测集。

---

## 0. 一句话目标

在 **shallow + Tw=3s + OpenBMI S3 权重** 固定前提下，用 **Stieger** 按原伪在线 **01–06 的协议形态** 各复现一版，得到与历史六臂**同结构、不同评测库**的对照表。

---

## 1. 公共冻结（六子臂共用）

| 项 | 取值 |
|----|------|
| 模型 | **仅 shallow**（`ShallowFBCSPNet`） |
| 窗长 / hop | **\(T_w=3\,\mathrm{s}\)** · hop=**100 ms** → `n_times=750` |
| 预训练 / 零样本权重 | OpenBMI Acc_paper **S3（5070 五折）**；出数前待填 `run_…`；对照 5060=`run_20260821_190504` |
| **伪在线数据** | **仅 Stieger 2021** → `stieger_3s_hop100` |
| 通道 | 与 OpenBMI 同序 · **无游戏重排** |
| 指标 | 试次/段级 **Acc_paper**；附报窗级 BalAcc、门控 abstain |
| **明确不用** | 游戏 sub02/sub03；BCI2a 作评测流；历史 01–06 权重目录（除非作只读对照） |

权重根：

`code/train_lab/out/5070_baseline_openbmi_3s_hop100_accpaper/shallow_openbmi_3s_hop100_balbatch_accpaper/openbmi_3s_hop100/run_<待填>/`

数据根：

`code/preprocess_lab/out/stieger_3s_hop100/` · `(N,1,8,750)` · prefix=`stieger`

Stieger 切段（与 `pipeline_3s_hop100` 一致）：

| 段 | 定义 |
|----|------|
| Task | 左/右反馈 `[fb0,fb1)`；反馈 **≥ 3 s**；丢 up/down、artifact |
| Rest | Cue 前 **4 s**（可缩短）；**不用** down 当 Rest |

---

## 2. 与历史 01–06 的映射（换数据集，不换协议意图）

| 本套代号 | 对齐原臂 | 原臂评测数据（**本套不用**） | 本套 Stieger 协议 | 产物子目录（建议） |
|----------|----------|------------------------------|-------------------|-------------------|
| **S08-01** | [01 不微调/零样本](../01_不微调_零样本/) | BCI2a 权重 × 游戏全会话 | S3 权重 × Stieger **全量** · **无门控** · 不微调 | `results/S08-01_zeroshot/` |
| **S08-02** | [02 前半训后半评](../02_微调_前半训后半评/) | BCI2a init × 游戏对半 FT | S3 **init** × Stieger **被试内 trial 时间对半**：前半全模型 FT、后半评；同后半零样本算 Δ | `results/S08-02_ft_half/` |
| **S08-03** | [03 teachable 门控](../03_旁路_teachable质量门控/) | 游戏 / BCI2a-P2 | 在 **S08-01** 流上叠 **H0–H3**（定义见 §3.3：无门控 / 硬阈值 / 只偏侧 / 试次内 top50%） | `results/S08-03_gate/` |
| **S08-04** | [04 OpenBMI 零样本+门控](../04_旁路_OpenBMI权重_游戏零样本与门控/) | OpenBMI 2s × 游戏 | **读数表**：**Q0**=无门控≡S08-01；**Q1**=开门控≡S08-03（本套不再另训） | `results/S08-04_q0q1_table/` |
| **S08-05** | [05 OpenBMI 前半 FT](../05_旁路_OpenBMI_前半微调后半评/) | OpenBMI 2s init × 游戏 | **协议同 S08-02**（init 已是 OpenBMI S3）；主报后半 FT vs 后半 ZS；产物可与 02 合并或软链 | `results/S08-05_ft_half/`（≡02 或 symlink） |
| **S08-06** | [06 FT+后半门控](../06_旁路_OpenBMI_前半FT后半门控/) | 05 FT × 游戏后半门控 | **只读 S08-02/05 的 FT ckpt**，后半叠 H0–H3；H0 应复现 05 后半 FT | `results/S08-06_ft_gated/` |

### 2.1 实质实验线（避免重复跑）

因本套 **固定 OpenBMI S3 + 仅 Stieger**，历史「换权重域」差异消失，六代号收成 **四条必跑线**：

| 必跑 | 覆盖代号 | 回答的问题 |
|------|----------|------------|
| **A. 全量零样本** | S08-01 / S08-04-Q0 | S3→Stieger 不微调能到多少？ |
| **B. 零样本+门控** | S08-03 / S08-04-Q1 | **硬阈值门控（H1）** 相对 **无门控（H0）**，Acc_paper 是否升高？ |
| **C. 前半 FT / 后半评** | S08-02 / S08-05 | 会话内标定是否仍有大 Δ？ |
| **D. FT 后再门控** | S08-06 | 微调后半再套 **H1（硬阈值）**，是否相对无门控再升？ |
| **E. Tw=2s 同库对照** | S08-01b / S08-02b（§3.5） | 离线 **3s−2s=+4.7 pp** 的窗长增益，跨库伪在线是否仍在？部署该切 3s 还是留 2s？ |

登记时仍按 **S08-01…06** 填表，便于和原 01–06 并排读。

---

## 3. 各子臂细则

### 3.1 S08-01（←01）

| 项 | 约定 |
|----|------|
| 权重 | S3 fold0–4 · Task / Three 头分别评 |
| 划分 | Stieger **全部保留试次**（按被试报） |
| 微调 | **无** |
| 门控 | **无**（≡ H0） |
| 五折含义 | 同一 Stieger 流 × 五套 OpenBMI 折权重 → mean±std |

### 3.2 S08-02 / S08-05（←02 / 05）

| 项 | 约定 |
|----|------|
| init | S3（OpenBMI 3s） |
| 划分 | 每被试合法 trial **时间升序对半**：前半只训、后半只评（同 02） |
| 微调 | 全参数 · Val Acc_paper 早停 · `max_epochs=300` · `patience=20` · `lr=1e-4` · balbatch |
| 对照 | **同后半零样本**（不微调 S3）算 Δ；**禁止**用全量 S08-01 减后半 FT |
| 05 相对 02 | 历史差在 init 域（BCI2a vs OpenBMI）；本套 init 已统一 S3 → **只跑一版，两代号同数** |

### 3.3 S08-03 / S08-04（←03 / 04）· **质量门控（写全）**

#### 3.3.1 门控在干什么（人话）

模型对每个 **3 s 滑窗** 先出预测，再按试次/段把多窗投票成 Acc_paper。  
**质量门控** = 投票前先丢掉「看起来不像可靠 MI」的窗，只让留下的窗参与 >50% 正确率判定。

- **不改**模型权重、**不改** Acc_paper 公式，只改「哪些窗进分母」。  
- **REST（静息）窗默认全部保留**；门控只筛 **Left/Right（MI）窗**。  
- 某试次筛完后 **0 窗可投** → 该试次记 **abstain（弃权）**：不进 Acc_paper 分母，但要附报弃权率。

本套对齐历史 03-P2 / 04 的 **在线门控**（可在 Stieger 流上当场算，不查 OpenBMI 离线清单）。

#### 3.3.2 字母代号一览（本臂会用到的）

| 代号 | 全称含义 | 一句话 |
|------|----------|--------|
| **Q0** | 04 的「无门控」读数位 | = 零样本、所有窗都投票 ≡ **H0** ≡ **S08-01** |
| **Q1** | 04 的「开门控」读数位 | = 同一套零样本流上扫 **H0–H3** ≡ **S08-03** |
| **H0** | 无门控对照 | 所有窗都进投票（基线） |
| **H1** | **主部署门控**（硬阈值） | MI 窗须同时满足：对侧 μ-ERD ≤ −15 **且** laterality ≥ 8 |
| **H2** | 放宽门控 | 只要求 laterality ≥ 8（不卡 ERD） |
| **H3** | 相对门控 | 每个试次内，MI 窗按 laterality 取 **top-50%** 再投票 |
| **abstain** | 弃权 | 门控后该试次无可投窗 → 不计分母 |

历史 03-P1 还有 **G0–G3**（OpenBMI 离线 mask：G0=无门控，G1=高 laterality 明显子集，G2=teachable 清单，G3=试次内 top-p%）。  
**H1 ≈ 当时 G2 的阈值**，但 G 系依赖离线名单，**Stieger 复现只用 H0–H3，不跑 G0–G3**。

#### 3.3.3 每个窗怎么算「质量分」（与 03/04 同公式）

对每个 **MI（左/右）窗**（质量特征在 **未做推理 z-score** 的窗上算；推理仍可按原协议 z-score）：

1. 取 **同试次 REST 段** 窗作基线功率 \(P^{\mathrm{rest}}\)（μ 带，约 8–12 Hz；通道 C3/C4）。  
2. 算该 MI 窗功率 \(P^{\mathrm{mi}}\)。  
3. 各通道：\(\mathrm{ERD}_c = 100\cdot(P^{\mathrm{mi}}_c - P^{\mathrm{rest}}_c)/P^{\mathrm{rest}}_c\)（负值 = 相对静息抑制，符合 ERD 直觉）。  
4. 期望：左手 → 对侧 **C4**；右手 → 对侧 **C3**。  
5. \(\mathrm{laterality} = \mathrm{ERD}_{\mathrm{同侧}} - \mathrm{ERD}_{\mathrm{对侧}}\)（越大 = 对侧抑制更强、偏侧越好）。  
6. **H1 过门**：\(\mathrm{ERD}_{\mathrm{对侧}} \le -15\) **且** \(\mathrm{laterality} \ge 8\)。

| 门控 | MI 窗保留条件 | REST 窗 |
|------|----------------|---------|
| **H0** | 全部保留 | 全部保留 |
| **H1** | ERD对侧≤−15 **且** lat≥8 | 全部保留 |
| **H2** | 仅 lat≥8 | 全部保留 |
| **H3** | 该试次 MI 窗中 laterality **最高的 50%** | 全部保留 |

#### 3.3.4 S08-03 / S08-04 怎么跑、怎么报

| 项 | 约定 |
|----|------|
| 权重 | 同 S08-01（S3 零样本，不微调） |
| 评测流 | Stieger **全量**试次（与 01 同一批窗） |
| 必报门控 | **H0、H1、H2、H3** 四行；主决策看 **H1 vs H0** |
| S08-04 表 | 只做读数汇总：**Q0 = H0 = S08-01**；**Q1 = S08-03 的 H0–H3 表**（不再另训） |
| 附报 | 各门控 abstain 率；可选「abstain 计错」灵敏度 |
| 成功线 | 分被试 Task 或 Three Acc_paper：**H1 − H0 ≥ +0.02** → 门控可候选；abstain 写入表（历史游戏约 16–21%，Stieger 以实测为准） |

主问题（人话）：**在 Stieger 上，丢掉「弱 MI」窗之后，段级准确率会不会比「全窗投票」更高？**

### 3.4 S08-06（←06）· **微调后再门控**

| 项 | 约定 |
|----|------|
| 权重 | **只读** S08-02/05 训好的各被试 fold FT `best_*.pt`（**禁止**重训） |
| 评测 | **仅后半** trial（与 05 评测段一致） |
| 门控 | 与 §3.3 **同一套 H0–H3 定义与公式** |
| 对照关系 | **H0** 应 ≈ S08-05 后半 FT（无门控）；再看 H1/H2/H3 |
| 成功线 | **H1 − H0**：Three **≥ +0.02**（Task 分人看，不强行全员同向） |

主问题（人话）：**会话内微调已经抬过一轮之后，再丢弱窗，还能不能再抬一点？**

### 3.5 S08-01b / S08-02b · **Tw=2s 同库对照臂**（v1.1 新增 · 必跑线 E）

#### 3.5.1 动机

离线 OpenBMI 上 3s 比 2s 高 **+4.7 pp**（Task 0.7415 vs 0.6941 · Three 0.5876 vs 0.5404），但那是**域内**数字；跨库场景整体掉 ~17 pp，窗长增益可能被域偏移吞掉。而 3s 窗在实时部署要多付 **1 s 延迟**。现状证据缺口：**2s 伪在线证据只在游戏侧（04，2 被试），3s 证据只在 Stieger 侧（07，24 被试），从未同被试同协议配对**。本对照臂补上这块，为「部署切 3s 还是留 2s」定价。

#### 3.5.2 臂定义

| 项 | S08-01b（零样本） | S08-02b（前半 FT） |
|----|-------------------|--------------------|
| 协议 | **同 §3.1 S08-01**，仅换窗长/权重/数据 | **同 §3.2 S08-02**，仅换窗长/权重/数据 |
| 窗长 / hop | **Tw=2s** · hop=100 ms → `n_times=500` | 同左 |
| 零样本权重 | OpenBMI **2s 正式 shallow** | 同左（作 FT init） |
| 伪在线数据 | `stieger_2s_hop100`（`(N,1,8,500)`） | 同左 |
| 门控 | **不跑**（H1/H2 为窗长无关生理指标；2s 基线若败，门控无意义） | 同左 |

**权重谱系（按优先级，2026-08-22 核实本机库）**：

| 档 | 权重 | 机位 | 说明 |
|----|------|------|------|
| **主选（本机已有）** | **5070 · 方案 18 L0（lib/braindecode）** `run_20260819_204409`：Task 0.6981±0.0353 / Three 0.5444±0.0263 | 5070 谱系 | `openbmi_2s_hop100` · balbatch · Acc_paper · **task/three × 5 折权重齐**（`5070_shallow_impl_audit_accpaper/`）；braindecode 骨干与 S3 同源 → 与 **5070 S3 构成全机位配对**，零同步零重训 |
| 备选 1 | 5070 · 方案 18 S0（self 自写） `run_20260819_162152`：0.6979±0.0356 / 0.5427±0.0243 | 5070 谱系 | 与 L0 已证等价（Δ≤0.17 pp）；可作 E 线复跑一致性附报 |
| 备选 2（部署谱系桥接） | 5060 正式 2s `run_20260807_135828`（0.6941 / 0.5404） | 5060 谱系 | **04 部署同源**；本机暂缺需同步；仅在需要与 07（5060 S3）**同机位严格配对**时使用 |

> 方案 18 审计结论（L0≡S0，logit 差 1.4e-6）保证任一档都不引入实现混杂；离线 5070 L0 与 5060 正式 2s 差 ≤0.4 pp（跨机先例量级），机位效应标注即可。

#### 3.5.3 预处理（一次）

入口已存在：`code/preprocess_lab/src/datasets/stieger/batch_2s_hop100.py` → `out/stieger_2s_hop100/`。切段与 3s 管线一致（Task=左/右反馈段、Rest=Cue 前 4 s）；须同样产出 `X_noz`（保持产物结构一致，虽然 b 臂不用门控）。

#### 3.5.4 对照读法（两级）

| 级别 | 对比 | 混杂控制 |
|------|------|----------|
| **主判（全 5070 配对）** | S08-01b（L0 2s）vs **S08-01**（5070 S3）；S08-02b vs **S08-02** | 同机位、同骨干谱系、同评测协议 → 最严配对，**判定以此为准** |
| **桥接（可先行出数）** | S08-01b（L0 2s）vs **S07-01**（5060 S3，只读：Task 0.5789 / Three 0.4198）；S08-02b 的 FT Δ 对照 S07-02/05（Three 0.6590） | 权重机位不同（5070 vs 5060），先例量级 ≤0.4 pp，标注即可；5070 S3 three 头出数前即可先出桥接读数 |

#### 3.5.5 预注册判定（升级原 §4 可选行）

以 **ΔThree = S3 − S2**（24 被试配对）为主判据，零样本与 FT 后取**弱者**：

| 结果 | 判定 | 行动 |
|------|------|------|
| ΔThree ≥ **+0.03**（且被试达标数 ≥ 16/24） | 窗长增益跨域成立 | **部署切 3s**（接受 +1 s 延迟）；09 号 OTTA 以 3s 为载体 |
| ΔThree < **+0.01** | 窗长增益不迁移 | **部署维持 2s**（省 1 s 延迟）；3s 仅作离线口径 |
| [+0.01, +0.03) 灰区 | 待裁定 | 看 FT 后方向 + Task 一致性 + 被试达标数；必要时补 5070 全机位配对（备选 2 权重） |
| 零样本有差、**FT 后差 ≤ +0.01** | FT 吸收窗长效应 | 2s+标定 ≈ 3s → **选低延迟的 2s 部署** |

**附带登记**：两窗长每 trial 窗数（2s 约 2 倍于 3s，Acc_paper 投票基数不同，写进表注）；b 臂不与游戏 04 的 2s 数字直接相减（评测库不同）。

---

## 4. 成功线总表

| 对比 | 门槛 | 对应必跑线 |
|------|------|------------|
| S08-01 vs 机会水平 | 报绝对值 | A 全量零样本 |
| **S08-01b/02b：S3 − S2（同库 Stieger）** | **≥ +0.03 且 ≥16/24 被试** → 窗长跨域成立、部署切 3s；**< +0.01** → 维持 2s 部署（详见 §3.5.5） | **E Tw=2s 对照** |
| S08-03：**硬阈值门控 H1** vs **无门控 H0** | 分被试 Acc_paper **≥ +0.02** | B 零样本+门控 |
| S08-02 FT vs **同后半**零样本 | 分被试 Task 或 Three **≥ +0.03** | C 前半 FT |
| S08-06：**H1** vs **H0**（均在 FT 后半上） | Three **≥ +0.02** | D FT 后再门控 |

**禁止**：与历史游戏 01–06 数字直接相减当增益（评测库不同）。

---

## 5. 工程与产物

### 5.1 预处理（一次）

```powershell
cd code/preprocess_lab
D:\cyy\MI\.venv\Scripts\python.exe -m src.datasets.stieger.batch_3s_hop100
```

### 5.2 建议跑序

包目录：`code/train_lab/src/step/5070_stieger_pseudo_online_3s_hop100/`（见该目录 `README.md`）

```powershell
cd code/preprocess_lab
D:\cyy\MI\.venv\Scripts\python.exe -m src.datasets.stieger.batch_3s_hop100

cd ..\train_lab\src\step\5070_stieger_pseudo_online_3s_hop100
$PY = "D:\cyy\MI\.venv\Scripts\python.exe"
$PY eval_zeroshot.py                          # S08-01 / Q0
$PY eval_gated.py --mode zeroshot             # S08-03 / Q1
$PY write_q0q1.py                             # S08-04 读数表
$PY ft_half.py                                # S08-02 ≡ S08-05
$PY eval_gated.py --mode ft                   # S08-06

# E 线（v1.1）· Tw=2s 对照：主选权重 = 5070 方案18 L0 run_20260819_204409（本机已有，零同步）
python -m src.datasets.stieger.batch_2s_hop100           # stieger_2s_hop100（一次，cd preprocess_lab）
$PY eval_zeroshot.py --tw 2s --weights run_20260819_204409  # S08-01b
$PY ft_half.py       --tw 2s --weights run_20260819_204409  # S08-02b（FT init = L0 2s 权重）
```

（`--tw 2s` 若包内暂无，为 E 线新增的最小改动：数据 tag、`n_times=500`、权重路径三处；**禁止** 2s 权重配 3s 窗或反之。）

烟雾：各脚本加 `--smoke`。

### 5.3 产物落点

全部写入本臂目录，**不写** `01_…`–`06_…`：

```text
资料/伪在线实验/08_旁路_OpenBMI_3s滑窗_Stieger零样本_5070/results/
  S08-01_zeroshot/
  S08-02_ft_half/
  S08-03_gate/
  S08-04_q0q1_table/
  S08-05_ft_half/        # 指向 02 的说明 md
  S08-06_ft_gated/
  S08-01b_zeroshot_2s/   # v1.1 · E 线零样本对照
  S08-02b_ft_half_2s/    # v1.1 · E 线前半 FT 对照
```

代码包：`code/train_lab/src/step/5070_stieger_pseudo_online_3s_hop100/`  
FT 权重：`code/train_lab/out/5070_stieger_ft_3s_hop100_accpaper/`（b 臂 FT 写 `..._ft_2s_hop100_accpaper/`）

门控依赖合并产物中的 **`stieger_X_noz.npy`**（与 `X` 同形、无窗内 z-score）。

---

## 6. 交付清单

- [x] 评测集 = **Stieger**；不用游戏/BCI2a 评测流  
- [x] **01–06 协议复现矩阵**写入本方案（S08-01…06）  
- [x] 预处理入口 `batch_3s_hop100` / `stieger_3s_hop100`（含 `X_noz`）  
- [x] 评测/FT 代码包：`5070_stieger_pseudo_online_3s_hop100`  
- [x] **v1.1**：S08-01b/02b 对照臂 + 判定线冻结（§3.5）；2s 预处理入口已存在（`batch_2s_hop100`）  
- [x] **E 线主选权重已在库**：方案 18 L0 `run_20260819_204409`（5070 · 五折双头权重齐，2026-08-22 核实）  
- [ ] 全量 `out/stieger_3s_hop100/`（依赖本机 mat）  
- [ ] A/B/C/D/**E** 五条必跑线正式出数  
- [ ] 结果登记表六代号 + b 臂填齐  
- [ ] （备选）同步 5060 2s `run_20260807_135828` 作部署谱系桥接——非必需，主选已定

---

## 7. 结论栏（跑后填）

| 代号 | 一句话结论 |
|------|------------|
| S08-01 | （待填） |
| S08-02/05 | （待填） |
| S08-03/04-Q1 | （待填） |
| S08-06 | （待填） |
| **S08-01b/02b（E 线）** | （待填：窗长 ΔThree=S3−S2=__ → 切 3s / 维持 2s） |

