# 方案：OpenBMI shallow · 前半微调 / 后半评（旁路）

> 状态：**已跑完 · 旁路结案** · 2026-08-09  
> 主记录：[`results/20260809_174914_shallow_openbmi_game_ft_half/`](results/20260809_174914_shallow_openbmi_game_ft_half/) · [`总结/结果登记表.md`](总结/结果登记表.md)  

> 臂名：`05_旁路_OpenBMI_前半微调后半评`  
> **角色**：在已采纳部署口径（[04](../04_旁路_OpenBMI权重_游戏零样本与门控/)）上，做与 [02](../02_微调_前半训后半评/) **同形** 的前半全模型微调、后半伪在线评。  
> **禁止**：覆盖 01/02/03/04 `results/`；改 Acc_paper 定义；跨被试合并；RAP/OTTA；只训分类头。

对照：

| 臂 | init | 评测范围 |
|----|------|----------|
| 02 | BCI2a Acc_paper Top-5 | 后半 trial |
| **05** | **OpenBMI shallow** `run_20260807_135828` | **同**后半 trial |
| 04 | OpenBMI shallow 零样本 | 全会话（无 FT） |

---

## 0. 一句话目标

回答：以 **OpenBMI 正式 shallow** 为 init，在游戏前半全模型微调后，后半 Acc_paper 相对「同后半零样本」是否仍有 Δ；并与 02·shallow（BCI2a init）对照（**分列，不夺冠**）。

---

## 1. 范围冻结

| 项 | 取值 |
|----|------|
| 模型 | **仅 shallow** |
| init | OpenBMI Acc_paper `run_20260807_135828` fold0–4 |
| 通道 | 训/评前重排游戏序 → OpenBMI 序 |
| 会话 | sub02 / sub03（同 01/02） |
| 划分 | 合法 trial 升序对半；前半只训、后半只评；trial 内 MI+REST 同进同出 |
| FT-Val | 前半内 `val_ratio=0.2` · `seed=42` |
| FT | **full_model**；`lr=1e-4` · `wd=1e-4` · `batch=32` · `max_epochs=300` · `patience=20` · balbatch · Val Acc_paper 早停 |
| 主报 | 后半段级 Acc_paper；附同后半零样本与 Δ |
| 设备 | `.venv` cu128 |

划分细则与泄漏禁令与 02 §2 **全文同文**（不重复改写）。

---

## 2. 通道重排（写死）

| 源 | 顺序 |
|----|------|
| 游戏 | `C3, C4, Cz, CP3, CP4, CPz, FC3, FC4` |
| OpenBMI | `Cz, C3, C4, CP3, FC4, FC3, CP4, CPz` |

`build_eval_stream` 之后、进模型之前 **统一重排**（训/Val/后半评/后半零样本共用同一 `X`）。

---

## 3. 成功 / 读数线（旁路）

| 对比 | 门槛 |
|------|------|
| 本臂 FT vs 同后半 OpenBMI 零样本 | Task 或 Three Δ ≥ **+0.03**（分人）→ FT 有增益 |
| 对照 02·shallow FT | 分列登记；**禁止**用两人均值夺冠；不改写 02 表 |

---

## 4. 工程

| 项 | 路径 |
|----|------|
| 实现包 | `code/train_lab/src/step/game_ft_openbmi_hop100_accpaper/` |
| 产物 docs | 本臂 `results/` · `out/` |
| FT 权重 | `code/train_lab/out/openbmi_game_ft_hop100_accpaper/` |

```bash
PY=D:/cyy/MI/.venv/Scripts/python.exe
cd code/train_lab/src/step/game_ft_openbmi_hop100_accpaper
$PY build_splits.py
$PY baseline_shallow.py
```

---

## 5. 明确不做

deep/其它模型矩阵；覆盖 02；模型训练 06 的 B2 头作 init；门控进本臂主表（后半门控见 [伪在线 06](../06_旁路_OpenBMI_前半FT后半门控/)）；改正式 5060 离线表。
