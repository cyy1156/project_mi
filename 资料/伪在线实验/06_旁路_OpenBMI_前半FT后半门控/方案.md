# 方案：OpenBMI 前半 FT · 后半 + 在线门控（旁路）

> 状态：**已跑完 · 旁路结案（Three H1+）** · 2026-08-09  
> 主记录：[`results/20260809_180505_shallow_ft_gated.md`](results/20260809_180505_shallow_ft_gated.md) · [`总结/结果登记表.md`](总结/结果登记表.md)  

> 臂名：`06_旁路_OpenBMI_前半FT后半门控`  
> **角色**：在 [05](../05_旁路_OpenBMI_前半微调后半评/) 已训好的 OpenBMI shallow 前半 FT 权重上，对**后半**评测叠加 teachable 在线门控（同 03/04 H0–H3）。  
> **禁止**：重训改 05；覆盖 01–05 `results/`；训练期门控筛窗；改 Acc_paper 定义；与「模型训练 06」混淆。

对照：

| 臂 | 权重 | 评测 | 门控 |
|----|------|------|------|
| 04 | OpenBMI 正式（零样本） | 全会话 | H0–H3 |
| 05 | OpenBMI → 前半 FT | 后半 | **无** |
| **06** | **05 冻结 FT ckpt** | **后半** | **H0–H3** |

---

## 0. 一句话目标

回答：05 微调后，再套 H1（≈G2）门控，后半 Acc_paper 相对本臂 H0（≡05 后半 FT）是否仍升。

---

## 1. 范围冻结

| 项 | 取值 |
|----|------|
| 模型 | **仅 shallow** |
| FT 权重 | 只读 05：`…/20260809_174914`（sub02/sub03 · task/three · fold0–4） |
| 划分 | 与 05/02 同：后半 trial 只评 |
| 通道 | 游戏序 → OpenBMI 序 |
| 门控 | 同 03-P2/04：质量窗无 z-score；同 trial REST 基线；H1=`ERD≤−15 & lat≥8`；REST 全留 |
| 主报 | 后半段级 Acc_paper；H0 应≈05 FT；附 abstain |
| 不做 | 前半训练时筛窗；重跑 FT；BCI2a；deep |

路径：

```text
code/train_lab/out/openbmi_game_ft_hop100_accpaper/
  shallow_openbmi_game_ft_half_balbatch_accpaper/20260809_174914/
    <subject>/{task,three}/fold{k}/best_{task|three}.pt
```

---

## 2. 成功线

| 对比 | 门槛 |
|------|------|
| H1 vs H0（本臂，分人） | Acc_paper ≥ **+0.02** → 门控在 FT 后仍可候选 |
| H0 vs 05 FT | 应接近复现（锚点校验） |

---

## 3. 工程

| 项 | 路径 |
|----|------|
| 评测 | `code/train_lab/src/step/game_pseudo_online_hop100/eval_ft_gated.py` |
| 产物 | 本臂 `results/` |

```bash
PY=D:/cyy/MI/.venv/Scripts/python.exe
cd code/train_lab/src/step/game_pseudo_online_hop100
$PY eval_ft_gated.py --gates H0,H1,H2,H3 --ft-stamp 20260809_174914
```

---

## 4. 明确不做

训练期 teachable 筛窗；覆盖 05；模型训练旁路 06 的 B2 头；改正式 5060 表。
