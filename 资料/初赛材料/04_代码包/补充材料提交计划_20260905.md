# 补充材料提交计划（XH-202610 · 2026-09-05 制定）
> **内部工作文件 · 不随作品提交**（含内部代号对照与过程记录，仅供团队排期使用）。

> **定位**：官方四件套之外的第 4 项「补充材料」没有强制清单，官方原文只规定了一条实质要求——
> 「若提交算法源代码，须以压缩包形式提交，明确标注编程语言、运行环境配置要求及代码使用说明，便于评审专家开展代码核查与性能复现验证」。
> 因此本计划 = **以源代码压缩包为主体 + 复现性证据随包附上**，直接支撑技术报告 20 分中的「可复现性」与整体可信度。
> **截止**：2026-09-10 邮件提交。本计划与 `README_初赛材料总览.md` §五排期对齐。

---

## 0. 执行状态（2026-09-05 更新）

| 步骤 | 状态 | 结果 |
|---|---|---|
| Step 1 README 增补 | ✅ 完成 | §4.2 与 §7 各增补一处 |
| Step 2 打包 | ✅ 完成 | `pack_source.py` 就绪；`源代码_XH-202610_20260905.zip` 已生成（1358 文件，3.5 MB，完整性 OK） |
| Step 3 脱敏抽查 | ✅ 完成 | 手机号/邮箱 0 命中；「身份证号」10 处均为长精度小数/three.js 常量误报；内部报告 md 残留已清除 |
| Step 4 附录 A/B | ✅ 完成 | 随包附录已入 zip；附录B 样例采用化名编号 syj0828，与报告/Excel 口径一致 |
| Step 5 终审 | ✅ 首轮通过 | 排他项 0 残留；README 引用的关键文件全部在包内；**9-08 仍按排期做最终复核** |
| Step 6 提交 | ⬜ 待 9-10 | 随邮件提交 |

补充说明：`config/ws_control_token.txt` 为本机操作台控制令牌（非个人密钥），为保证评审开箱即用予以保留。

---

## 一、补充材料包最终提交内容（目标形态）

```text
04_代码包/
├── 源代码_XH-202610_YYYYMMDD.zip      ← 主提交件（按 §三 清单打包）
│   ├── code/                          离线训练与评测源码
│   ├── collect_data/                  采集软件源码
│   ├── experiment_game/               在线系统源码+冻结配置（不含 data/subjects/）
│   ├── requirements.txt
│   └── README_代码包说明.md           ← 使用说明（已就绪，按本计划 §四 增补两处）
├── 附录A_旁路实验索引.md              ← 从 初赛材料包_v0/08 复制定稿（可选加分）
└── 附录B_自采数据质控样例.md          ← 1 个脱敏被试样例 + 质控勾选表模板（可选加分）
```

**不交**：DATA/ 公开数据集本体（数十 GB）、真实被试数据、模型权重（如评委需要另给网盘链接，见 §五 风险项）。

## 二、包内三要素自查（官方硬性规定）

| 官方要求 | 对应位置 | 状态 |
|---|---|---|
| 编程语言 | README §1（Python 3.11 ≥3.10；前端 HTML/JS） | ✅ |
| 运行环境配置要求 | README §1/§3（conda、torch 按 CUDA 装、requirements.txt） | ✅ |
| 代码使用说明 | README §2–§6（目录结构、安装、快速开始、冻结配置、落盘） | ✅ |

三要素已齐，剩余工作全部是**打包与增补**，无新写作量。

## 三、打包清单（照 `README_代码包说明.md` §7 执行）

**包含**：`code/`（train_lab、adapt_engine、preprocess_lab）、`collect_data/`、`experiment_game/`（源码+config+web+tools+`open_operator.bat`）、`requirements.txt`、`README_代码包说明.md`。

**排除**：
- `DATA/`、`experiment_game/data/`（真实被试数据；最多保留 1 个脱敏示例会话）
- `.venv/`、`__pycache__/`、`.git/`、`.idea/`、`.pytest_cache/`
- `sim_subjects/`、`_analysis/`、`_build/` 等中间产物

**预估体积**：源码 + 配置应在几十 MB 内；若超 200 MB 说明排除了什么漏了，回头查。

## 四、执行步骤（逐条勾选）

### Step 1（9-05，今天）README 增补两处
- [ ] §4.2 离线复现一节末尾加一句：混淆矩阵工具入口为 `cm_e1f_arms.py`，与《离线性能验证报告》sheet 08 原始数据索引一一对应。
- [ ] §7 打包清单后加一行「随包附录」：附录A（旁路实验索引）、附录B（自采质控样例），注明它们支撑报告 §12 可复现性声明。

### Step 2（9-05–9-06）制作打包脚本并生成 zip
- [ ] 在 `04_代码包/` 新建 `pack_source.py`（robocopy 排除规则见 §三；输出文件名 `源代码_XH-202610_<日期>.zip`）。
- [ ] 跑一遍，解包抽查：目录结构完整、无 `__pycache__`/`.git`、无被试真实数据。

### Step 3（9-06）脱敏与隐私合规抽查
- [ ] 全包搜索真实姓名/手机号/学号字段（`Grep`：姓名、电话、@qq.com 等）。
- [ ] 确认被试一律为编号标识（syj0828 等）；`experiment_game/data/` 未混入。
- [ ] 如保留示例会话：manifest 中无个人信息。

### Step 4（9-07）随包附录定稿
- [ ] 附录A：从 `资料/比赛要求/初赛材料包_v0/08_附录_旁路实验索引.md` 复制，路径改为压缩包内相对路径或删除本地绝对路径。
- [ ] 附录B：自采质控样例 = 1 个被试的质控勾选表 + 协议参数表（OpenBMI-Align v1），不含原始 EEG。

### Step 5（9-08）终审交叉检查
- [ ] README 中的命令逐条可读通（路径、文件名与实际包内容一致）。
- [ ] README 数字口径与 `README_初赛材料总览.md` §二 一致（QuadFold-59 / CausalFuse-8 / 0.511 / 61.88% 等）。
- [ ] zip 完整性：重新解压一次确认无损。

### Step 6（9-10）随邮件提交
- [ ] 邮件主题：`负责人－队伍名称－参赛单位－基于运动想象的脑-机交互算法研究与系统实现`
- [ ] 附件：技术报告 PDF + 离线验证 Excel + 演示视频 MP4 + 源代码 zip（+ 附录A/B 已在 zip 内则不单发）+ 报名表盖章扫描件。

## 五、排期与风险

| 日期 | 本计划事项 | 关联主线（总览 §五） |
|---|---|---|
| 9-05 | Step 1 + Step 2 脚本初版 | 采集战役继续 |
| 9-06 | Step 2 定稿 + Step 3 脱敏 | 录制演示视频 |
| 9-07 | Step 4 附录定稿 | 20 人收尾评测 |
| 9-08 | Step 5 终审 | 全套材料数字一致性抽查 |
| 9-10 | Step 6 提交 | 邮件提交 |

| 风险 | 对策 |
|---|---|
| 代码包内残留被试隐私 | Step 3 双重抽查（自动搜索 + 人工过目录）；发现即从源头删除重打包 |
| 评委想复跑但缺权重/概率 dump | README「已知限制」已声明；如被询问，提供网盘链接（登记表中有训练入口可重生成） |
| 打包漏文件导致复现命令跑不通 | Step 5 逐条对照 README 命令；`cm_e1f_arms.py` 所需产物路径在 README §8 已说明可重生成 |
| 与 9-08 数字终审冲突（采集还在回填） | 代码包不含数字性内容，仅 README 引用主数字——终审若改数，只需同步 README 一处 |

## 六、完成定义（DoD）

1. `源代码_XH-202610_*.zip` 生成且解包抽查通过；
2. README 三要素（语言/环境/使用说明）评委不看仓库也能照做；
3. 隐私抽查零命中；
4. 附录 A/B 就位；
5. 9-08 终审通过，进入提交队列。

---

*制定：2026-09-05 ｜ 依据：官方发榜文件「答题要求 4 / 补充材料」+ `README_代码包说明.md` §7 + `README_初赛材料总览.md` 排期*