{
  "ft_scope": "all4",
  "force_promote_on_gate_fail": true,
  "auto_promote_after_ft": true,
  "notes": "产品确认(2026-08-30)：门控 FAIL 仍强制晋升到 current（带 warning 日志）。so 为旁路。JSON 损坏时 fail-fast，禁止静默套用默认。"
}
