# 训练策略变更：二分类与三分类独立训练（不迁权重）

> 版本：v0.2（2026-07-27）  
> 状态：**代码已按本策略改**：三分类默认不迁权重；`--init-from-task` 仅历史对照  
> 适用：多主干基线对比（计划实验3）及后续过夜；历史「迁主干训头2」结果仍作对照归档，不再作为新实验默认。

---

## 0. 一句话

先独立训练 **静息/任务二分类**，完整记录结果；再 **从头随机初始化** 训练 **空闲/左/右三分类**。  
**禁止**加载二分类权重（含主干与分类器）；**不**统一各模型分类头，一律使用该模型 **自带分类器**。

---

## 1. 与旧策略对照

| 项 | 旧策略（已跑过夜，如 `141553` 等） | **新策略（本文）** |
|----|-----------------------------------|-------------------|
| 二分类 | 训 `n_outputs=2` 整网 | 同左：独立训二分类并记录 |
| 三分类 | 新建 `n_outputs=3`，**迁移**二分类主干（跳过分类层） | 新建 `n_outputs=3`，**完全不加载**二分类任何参数 |
| 分类器 | 各模型自带头（EEGNet 为 Conv 头） | **仍用各模型自带头**，不统一 Linear |
| 并行双头 | 未做 | 本阶段不做 |
| 串行门控 | 仅在线规划 | 本阶段不改训练结构 |

旧文档 `资料/模型训练/阶段B_第二分类头与权重迁移.md` 描述的是**迁移策略**，作历史对照；**新实验以本文为准**。

---

## 2. 训练流程（每个主干模型各自跑一遍）

```text
【阶段 Task】静息 vs 任务
  模型 = Backbone_X 自带分类头，n_outputs=2
  随机初始化 → 被试独立五折 → Val F1 早停/选型 → 记录 Test
  保存：本阶段权重与 summary（仅作二分类结论，不供三分类加载）

【阶段 Three】空闲 / 左 / 右
  模型 = 同一 Backbone_X 结构，自带分类头，n_outputs=3
  再次随机初始化（与 Task 权重无关）
  同一 seed / 同一被试五折划分 → Val F1-macro 早停/选型 → 记录 Test
  保存：三分类权重与 summary（独立目录）
```

过夜编排建议顺序（实现时改 `run_overnight_kfold.py`）：

```text
头1 网格 → 头1 调参 → 选优并写报告
→（清空迁移依赖）头2 网格/基线从零训 → 头2 调参 → 选优并写报告
```

注意：头2 **不再**需要 `task_kfold_dir` / `best_task.pt`。

---

## 3. 必须固定的实验变量

| 项 | 约定 |
|----|------|
| 数据 | 与对比实验一致（建议先 `merged_2s` 或单库 `*_2s`，全文声明） |
| 划分 | 被试独立五折，`seed=42`；合并库用按库分层 |
| 优化 | CrossEntropy + Adam；Val 选型，Test 终评 |
| 分类头 | **模型原生头**（不拆、不换成统一 Linear） |
| 双标签 | 仍用同一份 `X` + `y_task` / `y_three`（只是两个任务分开训） |

---

## 4. 报数与目录（建议）

每个模型单独目录，例如：

```text
code/train_lab/out/
  baseline_<model>_task_<stamp>/     # 仅二分类
  baseline_<model>_three_<stamp>/    # 仅三分类（无 init_from）
```

报告至少包含：

1. **二分类**：Val/Test Acc、F1（及合并库时 Overall / bci2a_only / stieger_only）  
2. **三分类**：Val/Test Acc、F1-macro（及按库拆分）  
3. 明确标注：`weight_transfer=false`，`classifier=native`

---

## 5. 代码改动（已落地）

| 文件 | 改动 |
|------|------|
| `models/registry.py` | 基线注册 + `build_model`；自研见 `models/custom/` |
| `train_task_kfold.py` | `--model` / `--data`；原生头 |
| `train_three_kfold.py` | 默认不迁权重；`--init-from-task` 可选 |
| `run_overnight_kfold.py` | `--model`；默认独立训三分类 |
| `run_baseline_matrix.py` | 多模型循环过夜 + overview 表 |

历史迁移代码保留开关 `--init-from-task`，默认关闭。

---

## 6. 拍板记录

| 项 | 决定 |
|----|------|
| 二分类 | 先训并完整记录 |
| 三分类 | 从头训，**不延用**二分类任何权重 |
| 分类器 | **不统一**，用各基线模型原来的分类器 |
| 并行双头 / 串行门控 | 本轮训练策略不做 |
| 代码 | **已落地**（见 `models/registry.py` 等） |

---

## 7. 相关文档

- **多模型 + 自研完整方案**：[`多模型基线与自研接入方案.md`](./多模型基线与自研接入方案.md)  
- 项目计划：`资料/项目计划/面向少样本个体适配的运动想象脑机接口系统设计.md`（§3.1.3 / 实验表已同步本策略）  
- 旧迁移教程（历史）：`资料/模型训练/阶段B_第二分类头与权重迁移.md`  
- 评估协议：`资料/模型训练/正式评估协议_被试独立五折.md`（划分规则不变；训练依赖关系见本文）  
- 合并库结果（旧迁移策略跑出）：`技术方案A_简单拼接合并合训.md` §10  
