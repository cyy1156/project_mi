# 比赛项目审查 · 差距分析与优化建议（v1.1 · 官方口径）

> 日期：2026-08-24 · 审查对象：全仓库（experiment_game / code/adapt_engine / code/train_lab / 资料/）  
> 性质：仅审查与方案建议，不改代码（遵约定）  
> **v1.1（2026-08-24 晚）变更**：对照基准换为**官方发榜文件** `资料/比赛要求/XH-202610_基于运动想象的脑－机交互算法研究与系统实现(7).pdf`（挑战杯"揭榜挂帅"·中国智慧工程研究会）。v1.0 以我方技术方案文本为基准得出的"五处合规偏差"（机械臂/4s 窗/分组粒度）**经官方文件核对均不成立**——官方未规定这些机制；相关条文是我方方案自述，已于 08-24 按 v2 标准改写（见 `游戏方案_定稿_v3_20260824.md` §11）。

---

## 0. 官方硬性要求（XH-202610 · 逐条摘录）

| # | 官方要求 | 出处 |
|---|----------|------|
| R1 | 三类（左手/右手/空闲）**或更多**认知信号的精准分类与实时识别；提交可验证、可演示的完整系统 | 三·攻关任务 |
| R2 | **自行采集 ≥20 人**训练数据佐证有效性 | 三·攻关任务 |
| R3 | 技术报告 **PDF**：特征提取方法、分类模型架构、**异步交互协议**设计原理、采集标准规范、验证流程与评价指标、实验数据与分析、参考文献 | 五·1 |
| R4 | 离线验证 **Excel**：须在**主办方指定的标准脑电数据集**上测试；标注**准确率、召回率、特异性、运算延迟**；附数据集使用说明、验证过程截图、原始验证数据 | 五·2 |
| R5 | 在线演示 **MP4 ≤10 分钟**：实时采集过程、三类实时分类结果、视觉反馈运行效果、状态切换响应性能 | 五·3 |
| R6 | 源代码（可选）压缩包：标注语言、运行环境、使用说明 | 五·4 |
| R7 | 评分：初赛 = **离线验证 60**（指定数据集准确率排序赋分）+ 技术报告 20 + 在线演示 20；决赛 = 新颖性 10 + 功能完整性 30 + **指定数据集准确率 30** + 应用价值 10 + 答辩 20 | 六 |
| R8 | **2026-09-10 前提交**（9-25 初审；10 月官方指导完善；11 月终审擂台赛） | 七 |
| R9 | 主办方提供：专业级脑机接口开发系统 + 脑电数据格式说明 + **刺激范式设计完整参数** + 系统接口 API 文档 | 九·2 |

**关键解读**：官方**没有**规定机械臂/第三人称、秒判定、4 档位移、分组微调、4s 特征窗、三轮预实验、网球/书写动画——这些全部来自我方方案文本（部分疑引自主办方范式材料，即 R9）。因此 08-24 的 v2 对齐（第一人称 + D8 计分 + 轮级微调 + 3s 滑窗）**对官方要求零冲突**。真正的官方硬约束是 R1–R8，尤其 **R4（指定数据集）与 R8（17 天倒计时）**。

---

## 1. 逐条映射（官方要求 → 现状 → 差距）

| # | 要求 | 现状 | 差距 | 优先级 |
|---|------|------|------|--------|
| R1 | 三类实时分类 + 完整系统 | v2 全链已编码（`trial_v2`/`inference_v2`/`session_v2`/`adapt_engine`）；串行门控 = 控制/空闲异步判别；M2 回放验收过 | 真机闭环未签字（L0 未跑/L1 未签/L2 未做）；修复清单未清（定稿 §10：致命 2 + 高 5） | **P0** |
| R2 | ≥20 人自采 | 真机 **2 人**（sub02/sub03） | 差 18 人 ≈ 4–5 采集日；流程/登记表/切窗已就绪 | **P0** |
| R3 | 技术报告 PDF（含异步协议） | 骨架可用系统设计 v2.0 + 定稿；实验数字齐（零样本 0.42→FT 0.66、爬坡曲线、门控 +3~4pp） | 未按官方要素成文；**异步交互协议**需独立成章（串行门控 + D8 早停/熔断即我方异步协议，是现成卖点）；参考文献未整理 | P1 |
| R4 | **指定标准数据集**离线 Excel（准/召/特/延迟） | 🔴 **未启动**：当前离线数字全部来自 OpenBMI/Stieger/自采 | ① 确认主办方指定数据集是哪一个、是否已到手（疑随 R9 开发系统/范式材料发放；我方冻结通道序 `Cz,C3,C4,CP3,FC4,FC3,CP4,CPz`@250Hz 疑似即其规格）；② 跑通我方管线（通道序/采样率核对）；③ 补齐召回率/特异性（混淆矩阵直接导出）与实测延迟 | **P0（60 分项）** |
| R5 | MP4 ≤10min | 无 | 彩排录屏（定稿 §12.2-3）后剪辑 ≤10min；覆盖四要素（实时采集/三类实时分类/视觉反馈/状态切换响应） | P1 |
| R6 | 源代码压缩包（可选） | 仓库齐全但 **v2 全栈未提交** | 提交 git → 打包 + 环境说明（conda/依赖/5070 机位路径脱敏） | P1 |
| R7 | 评分结构 | — | 60 分在指定数据集准确率 → 见 R4；报告 20 分靠可复现性（常量表/登记表/负结果链是素材）；演示 20 分靠稳定性（熔断降级路径是保障） | — |
| R8 | **9-10 提交** | 今日 8-24，**剩 17 天** | §4 按倒计时重排；原 4 周排期作废 | **硬约束** |

**我方方案自述条文**（预实验三件套/第一人称游戏/轮级微调/多维特征记录）：非官方评分项，但报告须自圆其说——已按 v2 口径改写完毕（定稿 §11），报告沿用该口径即可。

---

## 2. 代码审查发现（v1.1 修订）

沿用 `游戏v2代码审查_20260824.md` 的 F 编号；v1.0 中三条"合规"发现的最终处置：

1. **未提交 git**（维持 **P0 第一件事**）：v2 全栈 + adapt_engine + orchestrator 接线均在工作区。
2. **v1/v2 双离线管道并存**（维持）：冻结 v2 唯一口径，v1 epochs 归档。
3. ~~机械臂口径冲突~~ → **已解决**：官方无此要求；按用户裁决以 v2 为标准，全库文档已对齐（定稿 §11）。
4. ~~4s/1000pt 窗被删~~ → **已解决**：4s 口径系我方方案自述，官方未规定；提交口径统一为 3s 滑窗并已在方案文本改写。
5. ~~分组微调粒度~~ → **已解决**：官方未规定；轮级为 v2 冻结决策（D2），报告按"组=排程平衡单位"陈述。
6. 旧 `game_ft_*` 硬编码 5060 路径（维持：标注归档）。
7. **实时延迟未实测**（**升级为 P0**）：R4 明确把"运算延迟"列为必报指标——单窗预处理+前向耗时、判定端到端延迟必须真机实测入表。
8. 登记小问题与 25-G1 三个 bug（维持：若用"域增广"作噪声鲁棒卖点需先修）。

---

## 3. 优化建议（17 天关键路径）

### P0-A · v2 真机签字 + 延迟实测（~2–3 天）

1. 立即提交工作区；
2. 按定稿 §10 修 **F1–F7 + F14**（判定窗错位、auto_phase4、pilot 降级、rest 丢池、回放池、无效过滤、DriftGuard、D8 栅格复验）；
3. 合成板全流程（含早停/熔断/降级三条边角路径）→ 真人试点 → L2 签字；
4. 同场产出**延迟实测**（CPU/GPU 各一数字）→ 直接填 R4 表。

### P0-B · 20 被试采集战役（4–5 采集日）

前置门 = P0-A 签字。18 人按流程 v2.0（60–75 min/人，每日 ~5 人）；收工即评 + 切窗 + 登记表；**15 微调 + 5 留出**划分在集齐前冻结。

### P0-C · 指定标准数据集离线验证（~2–3 天，**60 分主体**）

1. 向主办方/范式材料确认指定数据集（R9 承诺提供格式说明与 API）；
2. 核对 8 通道序 @250Hz 与我方冻结口径是否一致；不一致则以该数据集规格重切窗（管线已参数化）；
3. 跑三个口径：S3 零样本、（若允许）我方微调后、在线回放判定——按官方指标**准确率/召回率/特异性/运算延迟**出 Excel 附截图与原始数据；
4. 若指定数据集 9-10 前无法到手：以自采 20 人 + 公开库双表提交并在报告说明（风险项，见 §4）。

### P1 · 提交材料（与采集并行）

- **PDF 报告**按 R3 要素重排：特征提取（3s 滑窗/CAR/notch 8–30/逐窗 z-score）→ 模型架构（ShallowFBCSPNet 双头）→ **异步交互协议专章**（串行门控控制/空闲判别 + D8 栅格加权计分早停 + 连 5 无效熔断降级）→ 采集规范 → 验证流程与指标 → 实验数据（爬坡曲线/门控增益/负结果链）→ 参考文献；
- **MP4 ≤10min**：彩排录屏剪辑，覆盖 R5 四要素；
- **代码包**：git 提交后打包 + 环境说明（R6）。

### P2 · 底座 v2（降级：移入 10 月完善窗口）

初审后官方 10 月安排指导完善（R8）——15 人微调底座 v2 + 5 人留出验证放该窗口，不为 9-10 提交强赶；提交版以 S3 + 个体微调链数字为准。

### P3 · 研究线处置

方案 26 降级（仅留 E1f 置信早停作在线策略候选）；25-G1 先修 bug 再谈；LeJEPA/KAN/模块类维持关闭（负结果链入报告作严谨性素材）；一律只走三接口（ModelRegistry/ReadoutPolicy/FTRecipe）。

---

## 4. 风险表（v1.1）

| 风险 | 等级 | 对策 |
|------|------|------|
| 指定数据集未到手/规格不明（60 分悬空） | **高** | 立即联系主办方（官方联络见 PDF §十一）；并行准备自采+公开库双表 |
| 17 天排期（修复 3 天 + 采集 5 天 + 材料 4 天 ≈ 无余量） | **高** | 修复与招募并行；材料用既有数字先行起草；底座 v2 移后 |
| 采集进度拖延 | 高 | 立即招募；降级路径（标定轮先行、游戏轮后补）已备 |
| v2 未签字就开采集 | 高 | P0-A 硬前置门；熔断降级回 v1 记录已实现 |
| 未提交代码丢失 | 高 | 今日内提交 |
| v1/v2 管道混用 | 中 | 冻结 v2 口径，v1 归档 |

## 5. 倒计时排期（8-24 → 9-10，共 17 天）

| 日期 | 主线 |
|------|------|
| 8-24/25 | git 提交 + F1–F7/F14 修复启动 + **联系主办方确认指定数据集** |
| 8-26/28 | 合成板/真人签字（L2）+ 延迟实测；被试招募到位 |
| 8-27/31 | 采集战役 18 人（每日 ~5 人）；并行起草 PDF 报告与 Excel 框架 |
| 9-01/03 | 指定数据集离线验证（到手即跑）；自采数据收工即评/切窗 |
| 9-04/07 | 底座/个体链数字回填；15+5 留出快速复核（可简化）；MP4 彩排+剪辑 |
| 9-08/09 | 材料终审（PDF/Excel/MP4/代码包）+ 报名表盖章流程 |
| **9-10** | **提交**（邮箱 772040863@qq.com，主题"负责人－队伍名称－参赛单位－选题名称"） |
| 10 月 | 官方指导完善窗口：底座 v2、增广、决赛打磨 |
| 11 月 | 终审擂台赛（决赛 30 分仍为指定数据集准确率 → 持续提升空间在此） |

---

**审查完毕（v1.1）。** 待项目组拍板两件事：① 指定标准数据集的确认与获取（R4 的 60 分前提）；② 17 天排期是否按上表执行（底座 v2 移后）。
