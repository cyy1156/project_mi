# 方案 P0：最小可验证闭环

## 1. 目的

工程上打通：**raw → 自写 Shallow `forward_features` → §3.2.1 切段 mean → Predictor → Expert_cur**，确认 `L_pred` 可降、分类不崩。  
科学对照上与 A2 接近；P0 更强调「实现里程碑」，A2 更强调「表格对照」。

## 2. 优先级

**必做**（写代码的第一目标）。

## 3. 模块开关

同 A2 核心；**禁止**只用全局向量做 `L_pred`；**禁止**接 Shallow 原 `final_layer`。

## 4. 配置（冻结）

| 项 | 值 |
|----|-----|
| Encoder | [`self_model/shallowfbcsp.py`](../../../../self_model/shallowfbcsp.py)，`n_times=1000`，**D=40** |
| 输入 | `(B, 8, 1000)` |
| 读出 | §3.2.1 → `I_vis` / `I_future` + mean |
| Predictor | D→2D→D，dropout 0.3；开跑 **D=40**（128 投影见 L1） |
| Expert_cur | D→C |
| target 路 | `torch.no_grad()` 编 `X_full` |
| λ_pred / λ_cls | 1 / 1 |
| SIGReg / Gate / Expert_future / Decoder | 关 |
| batch / patience | 256/512 / 20（OOM 见定稿 §3.0.1） |

## 5. 损失

`CE(p_cur) + λ_pred L_pred`

## 6. 验收

- `L_pred` 总体下降；Val **Acc_paper** 不低于 A1 过多
- 无 NaN；full 路确认无梯度
- 通过 §3.2.1 扰动单测

## 7. 结果记录

| 指标 | 值 |
|------|-----|
| Acc_paper mean±std | |
| L_pred 曲线备注 | |
