# 方案 A2：预测辅助 + 单专家推理

## 1. 目的

在 A1 基础上打开 **Predictor + L_pred（full 路 no_grad）**，但推理仍只用 `p_cur`，分离「表征预测辅助」与「双专家融合」。

## 2. 优先级

**必做**。可与 P0 共用代码，但报告必须单独一行。

## 3. 模块开关

| 模块 | 训练 | 推理 |
|------|------|------|
| `X_mask` / `X_full` | 都用；`(B, 8, 1000)` | 仅 `X_mask` |
| Encoder | **自写** `self_model/shallowfbcsp.py` + `forward_features` | 开 |
| target 路 | **`torch.no_grad()`** | 关 |
| Predictor + L_pred | 开 | 可前向但**不用**其分类 |
| Expert_cur | 开 | 开 → 唯一决策 |
| Expert_future / Gate / Decoder / SIGReg | 关 | 关 |

## 4. 配置

| 项 | 冻结开跑 |
|----|----------|
| 读出 | §3.2.1 + segment mean；开跑 **D=40**（`40→128` 见 L1 必做消融） |
| Predictor | `D→2D→D` |
| λ_pred / λ_cls | 1.0 / 1.0 |
| full 路梯度 | **关（no_grad）**；开见 B2 |
| 头任务 | Task + Three |
| batch / patience | 256/512 / 20 |

## 5. 损失

`λ_cls * CE(p_cur) + λ_pred * L_pred`

## 6. 对比读法

- A2 − A1：预测辅助是否涨点
- 相对 P1：缺双专家与 Gate

## 7. 结果记录

| 指标 | 值 |
|------|-----|
| Acc_paper mean±std | |
| vs A1 Δ | |
