# 2026-07-27 上午：单库过夜实验结果汇总

> 整理日期：2026-07-27  
> 范围：今早两轮 `run_overnight_kfold.py`（先 BCI2a，再 Stieger）  
> 性质：结果归档小结；详细折表见原始 MD。  
> **本文件不改代码。**

---

## 1. 当天背景（与方案 A 的关系）

今早在统一 **2s → 500@250Hz**、根目录 `.venv`、被试独立五折协议下，分别对两个公开库跑完整过夜：

1. **BCI2a**（`out/bci2a_2s`）
2. **Stieger**（`out/stieger_2s`，训练入口已切到 `stieger_*`）

这两轮是后续 **方案 A 合并合训** 的单库基线对照。合并过夜尚未开始。

---

## 2. 实验一：BCI2a 过夜

| 项 | 内容 |
|----|------|
| 时间戳 | `20260727_102818` |
| 开始 / 结束 | `10:28:18` → `10:35:27`（约 7 分钟） |
| device | `cuda` |
| 数据 | `code/preprocess_lab/out/bci2a_2s/bci2a_*.npy` |
| 切窗 | Cue 后 2~4s 任务 / Cue 前 2s 静息 → `(N,1,8,500)` |
| 规模 | N=1719，9 人 `A01–A09` |
| 权重目录 | `code/train_lab/out/overnight_20260727_102818/` |
| 完整记录 | [`../模型训练/五折过夜实验记录_20260727_102818.md`](../模型训练/五折过夜实验记录_20260727_102818.md) |

### 流程

顺序与 overnight 默认一致：

```text
头1 四组网格 → 选 Val 最优 → 头1 微调轮 → 选优
→ 迁移头1 权重训头2 基线 → 头2 调参轮 → 选优
```

### 头1 网格 Val F1（选型用）

| 网格 | 主要超参 | Val F1 mean±std | Test F1（仅报告） |
|------|----------|-----------------|-------------------|
| G1 | lr=1e-3, drop=0.50, wd=1e-4, pat=15 | **0.8247 ± 0.0201** | 0.7960 ± 0.0398 |
| G2 | lr=7e-4, drop=0.55, wd=1e-4, pat=18 | 0.8212 ± 0.0099 | 0.7504 ± 0.0578 |
| G3 | lr=5e-4, drop=0.60, wd=2e-4, pat=20 | 0.8237 ± 0.0100 | 0.7859 ± 0.0536 |
| G4 | lr=1.5e-3, drop=0.40, wd=5e-5, pat=15 | 0.8032 ± 0.0172 | 0.8067 ± 0.0318 |

网格初选：G1（Val 最高）。随后微调轮 **Run02** 略优，作为头1 最终推荐。

### 最终推荐（BCI2a）

**头1（静息/任务）· Run02**

| 指标 | mean±std |
|------|----------|
| Val F1 | **0.8262 ± 0.0160** |
| Test F1 | 0.7777 ± 0.0489 |
| Test Acc | 0.6898 ± 0.0276 |

超参：`lr=5e-4`，`weight_decay=1e-4`，`drop_prob=0.5`，`patience=20`，`max_epochs=100`，`batch_train=32`，`seed=42`

权重：`overnight_20260727_102818/02_task_tuned/`

**头2（空闲/左/右）· Run04**（迁移最优头1）

| 指标 | mean±std |
|------|----------|
| Val F1-macro | **0.5323 ± 0.0674** |
| Test F1-macro | 0.4454 ± 0.0985 |
| Test Acc | 0.4720 ± 0.0929 |

超参：`lr=5e-4`，`weight_decay=1e-4`，`drop_prob=0.65`，`patience=18`，`freeze_backbone=False`

权重：`overnight_20260727_102818/04_three_tuned/`

### 简评（BCI2a）

- 头1 Val/Test F1 在 0.78–0.83 量级，与此前 2s 五折量级一致。
- 头2 三分类仍明显更难（Test F1-macro ≈ 0.45），折间方差较大（9 人五折，val/test 人数少）。
- 时长短：样本量小，适合作为合并后 **BCI2a-only** 对照上限参考。

---

## 3. 实验二：Stieger 过夜

| 项 | 内容 |
|----|------|
| 时间戳 | `20260727_110155` |
| 开始 / 结束 | `11:01:55` → `12:32:07`（约 1.5 小时） |
| device | `cuda` |
| 数据 | `code/preprocess_lab/out/stieger_2s/stieger_*.npy` |
| 切窗 | 反馈段最后 2s → `(N,1,8,500)` |
| 规模 | N=22195，10 人（约 `S1…S11`，缺 S6） |
| 权重目录 | `code/train_lab/out/overnight_20260727_110155/` |
| 完整记录 | [`../模型训练/五折过夜实验记录_20260727_110155.md`](../模型训练/五折过夜实验记录_20260727_110155.md) |

### 头1 网格 Val F1（选型用）

| 网格 | 主要超参 | Val F1 mean±std | Test F1（仅报告） |
|------|----------|-----------------|-------------------|
| G1 | lr=1e-3, drop=0.50, wd=1e-4, pat=15 | 0.8438 ± 0.0159 | 0.8327 ± 0.0169 |
| G2 | lr=7e-4, drop=0.55, wd=1e-4, pat=18 | **0.8443 ± 0.0159** | 0.8301 ± 0.0202 |
| G3 | lr=5e-4, drop=0.60, wd=2e-4, pat=20 | 0.8424 ± 0.0145 | 0.8317 ± 0.0188 |
| G4 | lr=1.5e-3, drop=0.40, wd=5e-5, pat=15 | 0.8417 ± 0.0185 | 0.8240 ± 0.0184 |

网格最优：**G2**。微调轮未超过 G2 的 Val，故头1 最终取 **Run01 = 网格 G2**。

### 最终推荐（Stieger）

**头1（静息/任务）· Run01（= grid_2）**

| 指标 | mean±std |
|------|----------|
| Val F1 | **0.8443 ± 0.0159** |
| Test F1 | 0.8301 ± 0.0202 |
| Test Acc | 0.7165 ± 0.0237 |

超参：`lr=7e-4`，`weight_decay=1e-4`，`drop_prob=0.55`，`patience=18`，`max_epochs=100`，`batch_train=32`，`seed=42`

权重：`overnight_20260727_110155/00_task_grid_2/`

**头2（空闲/左/右）· Run04**

| 指标 | mean±std |
|------|----------|
| Val F1-macro | **0.4357 ± 0.0375** |
| Test F1-macro | 0.3992 ± 0.0076 |
| Test Acc | 0.4192 ± 0.0191 |

超参：`lr=1.5e-3`，`weight_decay=1e-4`，`drop_prob=0.5`，`patience=20`，`freeze_backbone=False`

权重：`overnight_20260727_110155/04_three_tuned/`

### 简评（Stieger）

- 头1 明显强于 BCI2a（Test F1 ≈ 0.83 vs 0.78），样本量大、折间更稳。
- 头2 Test F1-macro ≈ 0.40，低于 BCI2a 头2 的点估计，但折间 std 更小；两库头2 都未达实用阈值，需后续单独分析（类不平衡、范式差异等）。
- 过夜时长约 1.5 h，合并库预计同量级略长。

---

## 4. 两库并排对照（今早结论）

| 项目 | BCI2a `102818` | Stieger `110155` |
|------|----------------|------------------|
| N / 人数 | 1719 / 9 | 22195 / 10 |
| 头1 Val F1 | 0.8262 ± 0.0160 | **0.8443 ± 0.0159** |
| 头1 Test F1 | 0.7777 ± 0.0489 | **0.8301 ± 0.0202** |
| 头1 Test Acc | 0.6898 ± 0.0276 | **0.7165 ± 0.0237** |
| 头2 Val F1-macro | **0.5323 ± 0.0674** | 0.4357 ± 0.0375 |
| 头2 Test F1-macro | **0.4454 ± 0.0985** | 0.3992 ± 0.0076 |
| 头2 Test Acc | 0.4720 ± 0.0929 | 0.4192 ± 0.0191 |

读数注意：

- 两库 **不可直接比绝对难度**（范式、静息来源、每人体量不同）。
- 合并方案 A 跑完后，应以 **同一模型在 BCI2a-only / Stieger-only test 子集** 上的指标，对照上表单库 Overnight 的 Test 列。

---

## 5. 入口与环境（今早实际使用）

```text
环境：D:\cyy\MI\.venv
入口：code/train_lab/src/step/run_overnight_kfold.py
协议：被试独立五折 + 内层按人 val；调参只看 Val
张量：n_times=500 @ 250Hz
```

训练读数默认（Stieger 轮时后）：

- `train_task_kfold.DATA_DIR` → `out/stieger_2s`
- `train_three_kfold` 同
- 文件前缀 `stieger_*`

BCI2a 轮当时仍读 `out/bci2a_2s/bci2a_*`（该次 overnight 开始前的配置）。

---

## 6. 与方案 A 的衔接

| 已完成 | 待做（见 `技术方案A_简单拼接合并合训.md`） |
|--------|------------------------------------------|
| 单库 BCI2a 过夜基线 | 离线合并 → `out/merged_2s` |
| 单库 Stieger 过夜基线 | 分层五折 + 按库报数 |
| 文档归档（本文件） | 合并库过夜 + 对照小结 |

**当前指令：先不修改代码。** 确认方案 A 落地时再改合并脚本与训练入口。
