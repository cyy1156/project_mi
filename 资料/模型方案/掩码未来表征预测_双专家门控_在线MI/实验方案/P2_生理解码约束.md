# 方案 P2：训练定稿（含 Decoder · 唯一主结果）

> **#16**：与 [`../flowchart_A_train.png`](../flowchart_A_train.png) 一致——**训练有 Decoder**。

## 1. 目的

在 P1 上打开 Decoder + `L_dec`（PSD / μ / β + 时域），得到与训练流程图相同的完整方法。  
**论文 / 主表主结果 = 本方案。**

## 2. 优先级

**必做（唯一主结果）**；仅 Three。

## 3. 模块开关

| 模块 | 训练 | 推理 |
|------|------|------|
| P1 全部 | 开 | 同 P1 |
| Decoder | **开** | **关** |
| L_dec | 开 | 关 |

## 4. 配置

| 项 | 开跑默认 | 可扫 |
|----|----------|------|
| Decoder | Linear(D→8×400) → `(B, 8, 400)` | ConvTranspose（L1/附报） |
| λ_dec | **0.2** | `{0.05,0.1,0.2,0.5}`（L1） |
| L_dec 组成 | PSD+μ+β+时域(0.1)；**通道均值**后再 PSD/μβ | C 系列拆子项 |
| 其余 | 继承 P1；Adam；D=40 | |

## 5. 损失

`λ_cls*(CE_cur+CE_final) + λ_pred*L_pred + λ_sig*L_SIGReg + λ_dec*L_dec`  
（与训练图底部 `L_total` 一致）

## 6. 当前采用（L1 后填）

| 指标 | 值 |
|------|-----|
| Acc_paper mean±std | |
| vs P1 Δ | |
| λ / Gate 备注 | |
