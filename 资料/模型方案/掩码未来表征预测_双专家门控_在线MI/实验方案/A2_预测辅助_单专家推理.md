# 方案 A2：预测辅助 + 单专家推理

## 1. 目的

打开 Predictor + `L_pred`，推理仍只用 `p_cur`。

## 2. 优先级

**必做**（仅 Three）。可与 P0 共用代码，报告单独一行。

## 3. 开关

| 模块 | 训练 | 推理 |
|------|------|------|
| X_mask / X_full | 都用 | 仅 X_mask |
| Encoder | 自写 + `forward_features` | 开 |
| target | **no_grad** | 关 |
| Predictor + L_pred | 开 | 可前向但不用其分类 |
| Expert_cur | **`D→64→C`** | 唯一决策 |
| Expert_future / Gate / SIGReg / Decoder | 关 | 关 |

## 4. 配置

§3.2.1；D=40；λ_pred=λ_cls=1；Adam；batch 256/512；仅 Three。

## 5. 损失

`CE(p_cur) + λ_pred L_pred`

## 6. 结果

| Acc_paper | vs A1 Δ |
|-----------|---------|
| | |
