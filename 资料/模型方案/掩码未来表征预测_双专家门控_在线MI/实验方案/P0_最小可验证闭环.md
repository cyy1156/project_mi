# 方案 P0：最小可验证闭环

## 1. 目的

打通：自写 Shallow → §3.2.1 → Predictor → Expert_cur；确认 `L_pred` 可降。

## 2. 优先级

**必做**（仅 Three）。

## 3. 配置（冻结）

| 项 | 值 |
|----|-----|
| Encoder | `self_model/shallowfbcsp.py`，`n_times=1000`，D=40 |
| 输入 | `(B,8,1000)`；post-MI≥1.6s；缺 past 裁掉 |
| 读出 | §3.2.1：`I_vis=0..35`，`I_future=36..60` |
| Predictor | D→2D→D，dropout 0.3 |
| Expert_cur | **`D→64→C`** |
| target | `no_grad` 编 `X_full` |
| λ_pred / λ_cls | 1 / 1 |
| SIGReg / Gate / Expert_future / Decoder | 关 |
| 优化 | **Adam**；batch 256/512；patience 20 |

## 4. 验收

- `L_pred` 下降；Val Acc_paper 不太差于 A1  
- §3.2.1 单测通过：索引断言 + future 扰动 \(\|\Delta Z_{fut}\|/\|\Delta Z_{vis}\|\ge 3\)；**失败则停训**  

- full 路无梯度  

## 5. 结果

| 指标 | 值 |
|------|-----|
| Acc_paper mean±std | |
| L_pred 备注 | |
