# 串联流水线 + 单受试者验收

## 目标

把 Step1–11 收成 `preprocess_run`，对 **A01** 产出：

```text
X.shape == (n, 1, 8, 1000)
y_task  只含 {0, 1}     # 0=静息, 1=任务
y_three 只含 {0, 1, 2}  # 0=空闲, 1=左手, 2=右手
且满足映射：静息(0,0) / 左手(1,1) / 右手(1,2)
无 NaN；trial-wise 后每通道 std≈1
```

文件建议：`preprocess_lab/src/pipeline.py`  
脚本建议：`preprocess_lab/scripts/run_one_subject.py`

---

## 参考串联（左/右；静息留 TODO）

```python
import numpy as np


def preprocess_run(
    eeg: ContinuousEEG,
) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
    """返回 X, y_task, y_three。"""
    x = select_channels(eeg.x, eeg.ch_names)
    x = car_reference(x)
    x = notch_and_bandpass(x, eeg.fs)

    kept = filter_left_right_events(eeg.events, eeg.artifacts)
    # kept 每行: [cue, label_task, label_three, trial_index]
    xs, y_task, y_three = [], [], []

    for cue, lab_task, lab_three, _ in kept:
        ep = slice_epoch(x, int(cue), eeg.fs)
        if ep is None:
            continue
        ep = baseline_correct(ep, eeg.fs)
        win = classification_window(ep, eeg.fs)
        win = resample_to_1000(win, fs_in=eeg.fs, fs_out=250.0)
        if win.shape != (1000, 8):
            continue
        win = trial_zscore(win)
        xs.append(win)
        y_task.append(lab_task)
        y_three.append(lab_three)

    # TODO: extract_rest_cues → rest_window...
    #       追加时 y_task.append(0); y_three.append(0)；并做数量平衡

    if not xs:
        empty_x = np.zeros((0, 1, 8, 1000), np.float32)
        empty_y = np.zeros((0,), np.int64)
        return empty_x, empty_y, empty_y.copy()

    X = to_eegnet_tensor(xs)
    return (
        X,
        np.asarray(y_task, dtype=np.int64),
        np.asarray(y_three, dtype=np.int64),
    )
```

保存（三个文件对齐同一批样本）：

```python
np.save("out/A01_X.npy", X)
np.save("out/A01_y_task.npy", y_task)
np.save("out/A01_y_three.npy", y_three)
```

多 run 合并示例：

```python
def preprocess_subject(mat_path) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
    Xs, yt, y3 = [], [], []
    for eeg in load_bci2a_mat(mat_path):
        X, y_task, y_three = preprocess_run(eeg)
        if len(y_task):
            Xs.append(X)
            yt.append(y_task)
            y3.append(y_three)
    if not Xs:
        empty_x = np.zeros((0, 1, 8, 1000), np.float32)
        empty_y = np.zeros((0,), np.int64)
        return empty_x, empty_y, empty_y.copy()
    return (
        np.concatenate(Xs, axis=0),
        np.concatenate(yt, axis=0),
        np.concatenate(y3, axis=0),
    )
```

---

## 必须遵守的顺序

```text
选通道 → CAR → Notch+Bandpass →（按事件）切 Epoch
→ 基线 → 分类窗 → resample → trial Z-score → 堆张量
（标签：同一试次同时写出 y_task 与 y_three）
```

| 操作 | 位置 |
|------|------|
| CAR | 连续段、滤波前 |
| 基线 | 切 epoch 后 |
| Trial Z-score | 重采样后、进模型前 |

---

## A01 验收清单

- [ ] `X.shape[1:] == (1, 8, 1000)`
- [ ] `len(X) == len(y_task) == len(y_three)`
- [ ] `set(np.unique(y_task)) ⊆ {0, 1}`
- [ ] `set(np.unique(y_three)) ⊆ {0, 1, 2}`
- [ ] 一致性：`(y_three==0) ↔ (y_task==0)`；`(y_three∈{1,2}) → (y_task==1)`
- [ ] `np.isfinite(X).all()`
- [ ] 随机抽 5 个 trial：各通道 `std ≈ 1`，`mean ≈ 0`
- [ ] 左/右数量大致接近；若有静息，说明平衡策略
- [ ] （有旧四分类工程时）**不要**直接拿旧 `epochs_npy` 当本流水线输出

一致性自检示例：

```python
assert len(X) == len(y_task) == len(y_three)
assert np.all((y_three == 0) == (y_task == 0))
assert np.all(y_task[y_three > 0] == 1)
```

---

## 推荐学习完成度

1. 只跑 A01，左/右双标签 → 保存 `X / y_task / y_three`  
2. 加静息 `(0,0)` + 平衡  
3. 再加 gdf / csv loader  
4. 多受试者 + Step12 划分  

---

## 提交整条流水线检查时附上

1. `pipeline.py`（或等价串联代码）  
2. A01 的 `X.shape` / `y_task` 与 `y_three` 分布  
3. 任选 1–2 个 trial 的 mean/std  
4. 仍不确定的边界情况（静息、越界 cue 等）

AI 只会按本清单审阅并给修改建议，**不会直接改你的代码文件**。
