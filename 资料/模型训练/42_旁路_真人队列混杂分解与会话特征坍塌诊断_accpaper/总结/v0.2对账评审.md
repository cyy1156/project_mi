# 方案四十二 · v0.2 对账评审（不改数，只登记分歧）

> 对象：`总结/结果登记表.md`（Day0 产物，2026-09-05）与 `方案.md` v0.2 的差距  
> 性质：审读备注；全部结论待对表验证后才可采信

## 红旗 1（最高优先）：特征管线疑似未对齐，C/D 标签不可用

syj0828（线上窗级 0.924、队列最优）在 Day0 特征里 **d′=0.262、probe AUC=0.467（低于机会）**。线上读数最优的人不可能"无特征"——这指向 eeg.csv 重算特征与线上推理口径存在系统性错位（窗口锚、参考、预处理任一即可）。这正是方案 §6 风险行"抽 3 会话与 prob_dump 对表，差 >0.5 pp 冻结该特征"要拦的东西，但 Day0 直接跳过了对表。

**后果**：C 臂 weak_frac=0.908、D 臂"signal 层 11/16"等标签在通过校验前**全部不可信**，不得写入任何报告文字。
**动作**：抽 syj0828 / fnz0828 / zyj0902 各 1 会话，重算特征 vs `prob_dump`（或线上 v4_report 的窗判定）逐窗对表；对不上先修 `extract_features.py` 锚点。

## 红旗 2：A-full jackknife 与"A retrain pending"自相矛盾

登记表同时存在：
- "A · 混杂分解（观测代理）"：`R_vol_proxy=0.0588`，note **A retrain pending**；
- "A-full jackknife"：`R_vol_mean=0.0559`，verdict **volume_dominant**，还有 `dA5_mean_slope=0.0088`。

dA5 需要 A5 回放扫描**重训**才能算出；若重训未跑，这个数字来源不明。两个 R_vol（0.0588/0.0559）过于接近，怀疑后者由观测代理二次包装而来。
**动作**：核对 `arm_A_full.py` 产物 JSON 里每个 (person, seed) 的训练场次清单与 ckpt 路径；无重训产物则删除"A-full jackknife"节与 dA5 行，保留代理节并标 `proxy`。

## 红旗 3：D0 普查未按 v0.2 定义执行

v0.2 要求发生率统计在 **heldout 预测分布**（归一化熵 / heldout max_class_frac / MI 异侧率）上做，并给精确二项 95% CI 与决策门（上界 <20% 否 H0'；下界 ≥40% 全量展开）。当前 D 表用的是 per-person 训练侧 `max_class_frac` + d′ 混合标签，**决策门无法判定**。
**动作**：补 `arm_D0_census.py`（输入：各轮 heldout 窗级概率，已有 prob 类落盘或重放即可）；输出占比 + CI 后再走 §3 回填判定。

## 红旗 4（小）：口径与编码

- B 臂两处 n 不一致（混合模型 n=16，Spearman n=15）——缺一人的原因需注明（大概率某个体增益/质量字段缺失）；
- 登记表两处乱码行（"?????"）与标题错字（"坉塌"→"坍塌"）；
- 若为 UTF-8 写出问题，复用包内 `_bootstrap_utf8.py` 统一出口。

## Day0 中可以直接采信的部分

- **P0 身份合并**：16 个体（20 目录），fnz→fnz0828、cyy→cyy0830 主 ID，与方案一致；
- **B 臂负结果**：会话内疲劳"Rest μ 功率随试次序号上升"仅 56.2% 个体成立（n=16）→ 按 v0.2 判读："本范式时长（约 8 min/场）内未见会话内疲劳信号"，疲劳解释收窄到跨天状态；质量×增益 r=-0.061 → "质量非主要混杂"分支成立；
- **E 臂方向**：仿真坍塌占比 0.333 vs 真人 0.250（量级同源，支持"配方层因素非人专属"），待 D0 修口径后复算。

## 建议执行顺序（对齐 v0.2 §4）

1. 特征对表校验（红旗 1）→ 修管线；
2. `arm_D0_census.py` 补齐 → 走 D0 决策门；
3. 核实/重跑 arm_A_full（红旗 2），删除或证实 dA5；
4. 登记表清乱码、统一 n 口径；
5. 以上完成后再评 §3 回填判定表，决定 v4 报告哪些句子可改。

---

## 关闭记录（2026-09-05，全部红旗已处置）

| 红旗 | 处置结果 |
|------|----------|
| 1 特征管线疑似错位 | `feat_anchor_check.py`：生产锚点与自算锚点 AUC 一致（0.567/0.460、0.396/0.447、0.550/0.527）→ **无错位**；真相为手工 16 维特征族对 L/R 接近机会（底座直推同样低）。C/D 的 d′ 判据降级为"手工特征族内"，信号层证据改由 D0 承担 |
| 2 A-full 与 pending 矛盾 | A 臂确为真实重训（`arm_A_jackknife` 链产物含 overlay/release_gate/metrics），595 jobs 全部完成；旧 16 人口径的 `fnz`/`cyy` 目录归档至 `out/5070_exp42_confound_collapse/_archive_16person_A/` 后重聚合，n_people=17 |
| 3 D0 未按 v0.2 执行 | `arm_D0_census.py`（纯解析版）落地：末轮坍塌 1/17，CI95 [0.1%, 28.7%] → 中间带，D 臂缩为个案；熵以试次级 pred_hist 分布计算（免前向替代，已在 JSON 注明） |
| 4 乱码与 n 口径 | 源头修复（summary/arms_day0 的转义错字与丢字注释）；17 人口径重跑后 B 臂 n=17、Spearman n=16（一人增益字段缺失，已在登记注明） |

**最终判定**：R_vol=+3.6 pp、frac_pos=0.529 → mixed（部分贡献带）；R_span=−4.0 pp；dA5=−0.045 pp/%；末轮坍塌 1/17。回填判定已执行于 v4 报告（§3.7 新增、§4 混杂边界段、§5、附录 A）。
