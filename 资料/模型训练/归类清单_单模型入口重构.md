# 归类清单：单模型单入口重构（本步未改训练代码内容）

> 日期：2026-07-28  
> 原则：**不删除**旧代码；只做归类搬迁；**不修改**任何 `.py` 源码内容。  
> 新代码预留：`code/train_lab/src/step/baselines_single/`

---

## 1. 代码归类（已搬迁）

### 1.1 已移入 `code/train_lab/src/step/归档_旧训练入口/`

| 项 | 说明 |
|----|------|
| `models/` | 注册表 + 自研占位 |
| `run_five_baselines.py` | 五模型串跑 |
| `run_baseline_matrix.py` | 多模型矩阵 |
| `run_three_grid_backfill.py` | 三分类网格补跑 |
| `run_overnight_kfold.py` | `--model` + registry 过夜编排 |
| `train_task_kfold.py` | 旧五折壳（registry） |
| `train_three_kfold.py` | 同上 |
| `README.md` | 本归档说明 |

### 1.2 仍留在 `step/`（建议新脚本复用）

| 路径 | 说明 |
|------|------|
| `dataset.py` | 读 npy / Dataset |
| `metrics.py` | 指标与按库拆分报数 |
| `data_paths.py` | `merged_2s` / `bci2a_2s` / `stieger_2s` |
| `__init__.py` | 包标记 |

### 1.3 新计划目录（已建，尚无训练脚本）

`code/train_lab/src/step/baselines_single/`  
→ 后续手写 `shared_hparams.py` + 各 `baseline_*.py`

---

## 2. 结果归类（本机 `out/`，已在归档）

目标：`code/train_lab/out/归档_历史实验/`

当前含（示例）：

- `overnight_*` / `overnight_stieger_*`
- `kfold_task` / `kfold_task_stieger_2s`
- `stieger_2s/` 下历史 overnight
- 根级 `best_task.pt` / `best_three.pt`
- `README.md`

> `out/` 仍被 `.gitignore`；一般不进 Git。

---

## 3. 记录归类（已整理）

| 位置 | 内容 |
|------|------|
| `资料/模型训练/归档_过夜实验记录/` | 全部历史 `五折过夜实验记录_*.md`（含 eegnet/shallow/…） |
| `资料/模型训练/五折过夜实验记录_最新.md` | 入口指针，已改链到归档 |
| `资料/实验结果说明/归档_旧方案说明/` | `多模型基线与自研接入方案.md` |

仍留在 `资料/模型训练/` 的现行文档：

- 正式评估协议_被试独立五折.md
- 用处理好的数据训练EEGNet.md
- 代码示例_baseline_eegnet_单模型入口.md  ← **新：EEGNet 完整可粘贴示例**
- 阶段B_第二分类头与权重迁移.md
- 实验结果指标说明.md
- 名词对照_*.md
- 本归类清单

仍留在 `资料/实验结果说明/`：

- 训练策略_二分类与三分类独立训练.md
- 技术方案A_*.md
- 20260727_上午单库过夜实验结果.md
- README.md

---

## 4. 本步未做 / 文档同步

- 未改写任何训练函数 / 未新增 `baseline_*.py`
- 未删除旧代码（仅搬进归档目录）
- **文档**（2026-07-28）：已修正入口矛盾——现行说明指向 `baselines_single/` + 归档目录；历史过夜全文未改路径（见归档 README）
