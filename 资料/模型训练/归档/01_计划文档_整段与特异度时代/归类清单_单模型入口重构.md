> **【已归档】** 非现行主线。请改读 资料/模型训练/00_当前主线_1s伪在线/实验操作手册.md。

# 归类清单：单模型单入口重构

> 日期：2026-07-29（状态刷新）  
> 原则：旧代码**不删除**，已整文件搬入归档；新实验走 `baselines_single/`。  
> 现行入口：`code/train_lab/src/step/baselines_single/`

---

## 1. 代码归类

### 1.1 已移入 `code/train_lab/src/step/归档_旧训练入口/`

| 项 | 说明 |
|----|------|
| `models/` | 注册表 + 自研占位（现行入口**不用**） |
| `run_five_baselines.py` | 五模型串跑（历史） |
| `run_baseline_matrix.py` | 多模型矩阵（历史） |
| `run_three_grid_backfill.py` | 三分类网格补跑（历史） |
| `run_overnight_kfold.py` | `--model` + registry 过夜编排（历史） |
| `train_task_kfold.py` / `train_three_kfold.py` | 旧五折壳（registry） |
| `README.md` | 归档说明（**不可直接跑**，见该 README） |

### 1.2 仍留在 `step/`（新脚本复用）

| 路径 | 说明 |
|------|------|
| `dataset.py` | 读 npy / Dataset |
| `metrics.py` | 指标与按库拆分报数 |
| `data_paths.py` | `merged_2s` / `bci2a_2s` / `stieger_2s` |
| `__init__.py` | 包标记 |

> 注意：`step/models/` 若仅剩 `__pycache__` 属残留，**真 registry 在归档**；勿当作现行入口。

### 1.3 现行目录 `baselines_single/`（已落地）

| 文件 | 说明 |
|------|------|
| `shared_hparams.py` | 全基线共用超参 |
| `baseline_eegnet.py` 等五个 | 一模型一脚本；Task→Three 独立五折 |
| `run_all_five_model.py` | **可选**串跑；日常对照优先单脚本 |
| `README.md` | 运行约定与输出路径 |

示例：

```bash
cd code/train_lab/src/step/baselines_single
python baseline_eegnet.py --data merged_2s
```

权重：`code/train_lab/out/baseline/<model>/<data>/run_<stamp>/`  
MD：`资料/模型训练/runs/<stamp>_<model>/五折实验记录.md`

---

## 2. 结果归类（本机 `out/`，已在归档）

目标：`code/train_lab/out/归档_历史实验/`

当前含（示例）：

- `overnight_*` / `overnight_stieger_*`
- `kfold_task` / `kfold_task_stieger_2s`
- `stieger_2s/` 下历史 overnight
- 根级 `best_task.pt` / `best_three.pt`
- `README.md`

> `out/` 仍被 `.gitignore`；一般不进 Git。

---

## 3. 记录归类

| 位置 | 内容 |
|------|------|
| `资料/模型训练/归档_过夜实验记录/` | 全部历史 `五折过夜实验记录_*.md` |
| `资料/模型训练/五折过夜实验记录_最新.md` | 旧过夜指针 → 归档 |
| `资料/模型训练/五折实验记录_最新.md` | 新入口实验指针 → `runs/` |
| `资料/模型训练/runs/` | 现行单模型五折 MD |
| `资料/实验结果说明/归档_旧方案说明/` | 历史：注册表 + 多模型矩阵方案 |

仍留在 `资料/模型训练/` 的现行文档：

- 正式评估协议_被试独立五折.md
- 用处理好的数据训练EEGNet.md
- 代码示例_baseline_eegnet_单模型入口.md
- 阶段B_第二分类头与权重迁移.md（历史）
- 实验结果指标说明.md
- 名词对照_*.md
- 本归类清单

仍留在 `资料/实验结果说明/`：

- 训练策略_二分类与三分类独立训练.md
- 技术方案A_*.md
- 20260727_上午单库过夜实验结果.md
- README.md

---

## 4. 状态核对（2026-07-29）

- [x] 旧 registry / matrix / overnight / `train_*_kfold` 已归档（未删）
- [x] `baselines_single/` 五基线脚本 + `shared_hparams` 已落地
- [x] 文档入口说明已与现行代码对齐（本轮刷新）
- [ ] 归档目录若需复跑，须另修 `parents[]` / `sys.path`（当前默认不修）
